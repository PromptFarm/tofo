# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
*logs

# Готово. Сохранено в MEMORY.md со ссылкой на полный лог.
# В следующей сессии достаточно сказать "продолжаем с валидатором" — контекст подтянется автоматически. Следующий шаг: Fix A ("important decisions" → точный матч) + решение по Fix B (HIGH→MEDIUM для >4 опций у GD)